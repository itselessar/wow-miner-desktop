# Runtime binaries

Run `npm run vendor:fetch` on Windows before development or packaging. The
script places the verified official `wownerod.exe` and its upstream license in
this directory. Downloaded binaries are intentionally excluded from Git.

